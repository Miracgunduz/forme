DATABASE_URL="file:./dev.db"
PORT=4000

# Free key: https://fdc.nal.usda.gov/api-key-signup (DEMO_KEY works but is
# capped at 30 requests/hour, 50/day — fine for trying things out, not for real use).
USDA_API_KEY=DEMO_KEY
